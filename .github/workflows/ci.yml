name: ci

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Install bubblewrap
        run: |
          sudo apt-get update
          sudo apt-get install -y bubblewrap
      - name: Run test suite
        run: bash tests/run.sh

  shellcheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - name: Run ShellCheck on production scripts
        run: |
          set -eu
          shellcheck \
            install.sh \
            uninstall.sh \
            update.sh \
            doctor.sh \
            vigil-aliases.sh \
            scripts/run-pyszz.sh \
            scripts/hooks/pre-push \
            scripts/hooks/prepare-commit-msg

  python-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      - uses: actions/setup-python@0b93645e9fea7318ecaed2b359559ac225c90a2b # v5.3.0
        with:
          python-version: '3.12'
      - name: Install linters
        run: pip install ruff==0.15.12 bandit==1.9.4
      - name: Run ruff
        run: ruff check scripts/
      - name: Run bandit
        run: bandit --severity-level medium --confidence-level high scripts/vigil-review.py scripts/vigil-install-review
