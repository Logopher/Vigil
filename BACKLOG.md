# Backlog

Running list of known issues and proposed work, organized by triage disposition. Items here are *not* commitments — they are the current best-known set of "things to consider next" and are expected to churn. When something is acted on, remove it; when something is decided not worth doing, remove it with a note in commit history.

For project stage and what's blocking each transition, see `LIFECYCLE.md`. For verified vs. unverified protection claims, see `THREAT_MODEL.md`.

**Last triaged:** 2026-05-08 (refactored hook architecture into a single sudo-installed `vigil-hook` dispatcher at `/usr/local/bin/`, fixing silent hook-firing failure caused by user-level `settings.local.json` not being honored for hooks; closed direct JSONL session ID linkage with exact harness-UUID lookup in the sidecar writer; added adversarial tests for both validators in `tests/vigil-hook.sh`; integrated three new items proposed during the session — `vigil-report` static HTML generator, LLM enrichment layer with four cached passes, `vigil-pyszz` wrapper as a conditional in-session trigger; merged the now-vestigial "Observability — standalone use before Vigil integration" section into Analytics; added an architectural-framing paragraph to Analytics; updated 2026-04-26 incident remediation to reflect that `validate-settings-write` is now actually hard-enforced post-refactor and regression-tested; noted that `tools-<session>.jsonl` only accumulates from this point onward, so historical evidence of in-sandbox `filter-sandbox-denies.py` invocations isn't recoverable from logs alone; removed Install Vigil update, pyszz standalone, diff-to-session forward-direction attribution, and the standalone ccusage tool-recommendation entry as obsoleted or out-of-scope). Prior: 2026-05-06 (added OpenCode permission-layer port to Stage 2; added WSL2-on-Windows and agent compatibility docs to COMPATIBILITY.md; added multi-model third-defense-layer caveat to THREAT_MODEL.md). Prior: 2026-04-26 (shipped marker-file pattern enabling hook env-var workaround; shipped per-tool-call logging, active-policy banner, and memory-write validation hooks; removed all three backlog items; added Analytics section from teardown of current analytics layer gaps; added Security incidents section and two investigation items).

## Security incidents

Documented breaches with root-cause analysis and remediation status. Keep entries until the open investigation items are resolved, then move to commit history.

### 2026-04-26 — fewer-permission-prompts skill executed by sub-agents (TCard session)

**What happened.** A TCard session spawned general-purpose sub-agents. Both agents inherited the full session context including the `fewer-permission-prompts` skill definition. Without explicit invocation by the top-level agent, each agent scanned `~/.claude/projects/*.jsonl` transcripts and wrote allow entries to `~/code/TCard/.claude/settings.json` (the project-local settings file — the skill's intended target). The user approved the permission prompt provisionally while suspicious and investigating, then reverted after alerting the top-level Claude.

**Root cause.** Sub-agents spawned via the `Agent` tool inherit the full session context, including skill definitions loaded via `system-reminder`. A skill with imperative language ("scan… then add") is treated by a general-purpose sub-agent as an actionable instruction rather than a registered capability requiring explicit invocation. No Vigil-layer fix exists for this: project-local settings files must remain writable (the skill requires it), so file-path blocking would break legitimate use.

**Effective mitigations (operational, not enforcement).** Avoid spawning general-purpose sub-agents via the `Agent` tool in sessions where skills are loaded; the agents inherit skill definitions and may execute them without explicit invocation. For tasks that would otherwise spawn an agent to run a trivial shell command, `!` in the Claude Code prompt runs it directly in the session instead. The clearest signal of skill redirection: an agent returns a coherent structured report on a subject that was never assigned.

**Contributing factor (hypothesis — open investigation below).** `~/.claude/settings.json` is absent from the live sandbox `denyWrite` array despite being in `MASTER_DENY_WRITE` (`scripts/filter-sandbox-denies.py:83`). Entries whose target paths are null-mounted as `/dev/null` character devices fail the `is_file()` type check and drop silently. If `filter-sandbox-denies.py` ran in write mode (without `--check`) inside a sandboxed session at some point, the `~/.claude/` entries would have been dropped this way. This did not affect the incident's write target but represents independent exposure.

**Remediation applied (partial).** `validate-settings-write` hook is now hard-enforced as a `vigil-hook` subcommand (post-`b87386d`; the prior declaration in `settings.local.json` was silently inactive because user-level `settings.local.json` is not in Claude Code's hook-precedence list). Hard-denies Write/Edit/MultiEdit tool calls targeting `~/.claude/settings.json` and `~/.claude/settings.local.json`; regression-tested in `tests/vigil-hook.sh`. This addresses the independent sandbox exposure (global settings files only); the sandbox `denyWrite` gap for those paths remains open until `filter-sandbox-denies.py` is re-run outside the sandbox. **Nothing currently blocks a repeat write to `~/code/TCard/.claude/settings.json` or any other project-local settings file.** Root cause (skill execution by sub-agents) is unaddressed.

**Open investigation.**
- Identify when `filter-sandbox-denies.py` ran inside a sandboxed session. In `~/vigil-logs/`, look for a `tools-<session>.jsonl` file containing a `PreToolUse` Bash entry whose `command` field includes `filter-sandbox-denies.py`. Also check `git log` for hook or alias changes that could have shifted the invocation context. Until resolved, treat `denyWrite` coverage of `~/.claude/` paths as unreliable. Caveat: `tools-<session>.jsonl` files only began accumulating after `b87386d` (2026-05-08); pre-refactor sessions left no per-tool-call record, so historic in-sandbox invocations aren't recoverable from logs alone — only future ones are.

## Documented gaps — do not action

These are honest limits, not bugs to fix. Listed so a maintainer encountering odd behavior recognizes the symptom and stops.

### Platform support

- **macOS/BSD `script(1)` branching not runtime-tested.** `vigil-aliases.sh` adapts its `script(1)` invocation per `uname`, and Tier 6 statically verifies the case patterns cover the supported platforms, but the BSD branch has never run on a Mac/BSD host. The first such run is the test. `COMPATIBILITY.md` flags this honestly.
- **Windows (Git Bash / MSYS2) untested.** `prune-worktrees.sh` includes Windows path handling; the rest is unexercised.

### Test-suite limits

- **Matcher-semantics glob overlap between allow/deny is partially checked.** Tier 3 now catches exact-string conflicts plus dead-rule subsumption (policy allow/ask whose match set sits inside a profile deny), via `check_allow_deny_dead_rules` in `tests/semantics.py`. Two approximations remain documented in source: (a) path glob-on-glob subsumption classifies mutually-matching wildcard pairs as equivalent, so e.g. `Read({{HOME}}/**)` subsuming `Read({{HOME}}/.ssh/*)` is missed; (b) Bash space-form patterns (e.g., `Bash(git checkout -- *)`) are compared by exact token equality, with embedded `*` treated as a literal. Closing either requires a real matcher emulator or full glob-intersection solver — non-trivial.

## Next-session candidates

Each item is substantial enough to deserve its own session. Pick one, plan it, ship it, then return. Items are listed in dependency order where applicable — items earlier in the list unblock or motivate those below.

- **`update.sh` change-detection via install manifest.** Current update behavior is "freshly installed files always win": local edits to `~/.claude/CLAUDE.md`, policy files, or other Vigil-installed content are lost on update. New model: `install.sh` writes a SHA-256 manifest to `~/.config/vigil/.install-manifest`; `update.sh` compares pre-update hashes to manifest, preserves files that diverge from the recorded hash, stages the new version as `<path>.new` (or under `~/.config/vigil/pending-updates/`), and summarizes preserved paths at exit. Makes user customizations — including the `@~/.claude/USER_CONTEXT.md` import added to `~/.claude/CLAUDE.md` — survive updates without workaround. Useful independently; prerequisite for ergonomic default-profile-swap because promoted profiles need to participate in updates cleanly.
- **`vigil-sign-up` helper for one-shot signing-agent setup.** Signing runs through an `excludedCommands` carve-out rather than through the sandbox (see `DESIGN.md`), so the helper is no longer gating an otherwise-unreachable feature. It just automates the standard setup: generate a dedicated key (`ssh-keygen -t ed25519 -f ~/.ssh/vigil-signing -N ""`), upload the pub key to GitHub as a Signing Key via `gh`, start an agent (`ssh-agent -a /tmp/claude/vigil-signing.sock` + `ssh-add`), write `signing.env` with `SSH_AUTH_SOCK`, and confirm `user.signingkey`. Lower priority than when the sandbox path was assumed workable — a user with an existing host-side agent only needs the carve-out config line. Still worthwhile for first-time onboarding: idempotent on re-run, handles GitHub upload via `gh`, prints the blast-radius note from `THREAT_MODEL.md` before touching keys, writes all state to user-managed paths. Optional follow-on: systemd-user unit for auto-start on login. Plan first — interactive-prompt design and idempotent regeneration behavior are unchanged from the prior framing.
- **`vigil-corpus-check` referential-integrity scanner.** Resolves every `*.md` path reference in `~/planning/`, repo BACKLOGs, repo CLAUDE.mds, and `~/.claude/projects/*/memory/**`; reports broken ones (file doesn't exist, anchor/header doesn't match, line number out of range). Standalone CLI invocation initially; later integration into the commit-review gate as `vigil-hook validate-doc-refs` so dangling pointers can't survive a commit. Smallest reliable corpus-consistency win — would have caught the "consolidation plan in `~/planning/`" dangler at commit time rather than at session N+M. Strategic context: `~/planning/corpus-discipline.md`. Trigger: standalone usefulness now; gate integration once the scanner has been exercised against the live corpus for a few sessions.
- **`vigil-hook plan-triage` SessionEnd subcommand.** Surfaces the session's plan file at session close and prompts a four-choice triage decision: archive (still-useful reference; move to `~/.claude/plans/archive/`), delete (transient working artifact; remove), promote (content moves into a `~/planning/` canonical doc and/or `~/planning/DECISIONS.md` entry; plan file then archived or deleted), keep-active (in flight; no move). Honors the existing `~/.claude/plans/archive/` convention; never auto-deletes. Triage decision is logged for ground-truth into the Bakunawa hard-drive pipeline classifier (separate item in `~/code/bakunawa/BACKLOG.md`). Strategic context: `~/planning/corpus-discipline.md`. Plan first — prompt UX and integration with `~/.config/vigil/.vigil-session` (already used by other hooks) deserve design attention before implementation.
- **Memory-write hook slug-mismatch diagnostic.** Observed 2026-05-13: the `validate-memory-write` hook reports the session slug as `-home-grault-planning` when the working directory is `~/planning/`, but the existing memory entries live under `-home-grault` (the home-directory slug). The hook correctly blocks the cross-project write as designed, but the upstream slug derivation is inconsistent with the session-start system reminder that points at the `-home-grault` directory. Two unknowns to resolve: (1) which slug should be canonical for a session started in `~/planning/` — the working-directory-derived one or the home-rooted one; (2) whether the existing `-home-grault/memory/` should be relocated, aliased, or merged. Until resolved, durable-decision memory entries can't be written from `~/planning/`-based sessions, which makes corpus-discipline auto-memory entries (paired with each `DECISIONS.md` entry) impossible to land. Strategic context: `~/planning/corpus-discipline.md`. Diagnostic first: identify the slug-derivation logic in `vigil-hook validate-memory-write` and reconcile against the session-start advertisement.

## Analytics — Vigil layer improvements

Improvements to the analytics infrastructure already shipped in `scripts/` and `~/vigil-logs/`. Items are ordered so earlier ones unblock or simplify later ones.

Architectural framing: structured-data joins and aggregations are the deterministic layer (cost arithmetic, SHA matching, time-series, blame walks, retry-pattern detection — none need an LLM). LLM enrichment is a thin semantic layer on top, cached by stable IDs (session_id, commit pair) and only run on aggregated rows — never on raw `tools-<session>.jsonl`. Presentation (`vigil-report` HTML, future GUI, future `vigil` CLI) sits above both.

- **Verify `--live-pricing` model ID resolution against a real session.** ccusage uses LiteLLM's canonical dataset (`https://raw.githubusercontent.com/BerriAI/litellm/main/model_prices_and_context_window.json`); `join-sessions.py` now fetches the same URL via `--live-pricing`. LiteLLM keys use the form `anthropic/claude-<name>` while JSONL `message.model` values use bare `claude-<name>`. `_lookup_price` tries both forms, but this has not been exercised against a real session JSONL. One live run confirming the key resolution works — or identifying which additional aliases are needed — closes this item. Once verified, update `_DEFAULT_PRICING` comment to note it was last refreshed from LiteLLM on a given date.

- **Issue-tracker integration for pyszz date filtering.** `bug-fixes.json` supports `earliest_issue_date` per entry, which tells SZZ to exclude candidate inducing commits authored after the issue was first reported. `pyszz-bugs` currently omits this field. Enriching it requires correlating each fix commit to a GitHub Issue — either via issue references in commit messages (`fixes #123`, `closes #123`) or PR metadata — then fetching the issue creation timestamp via the GitHub API. Enables more precise inducing-commit attribution at the cost of a `gh`/API dependency and the assumption that fix commits reference their issues.

- **Tool-use aggregation script.** `scripts/summarize-sessions.py`: reads `tools-<session>.jsonl` files, outputs per-session summary rows — call count by tool type, Bash-to-Read ratio, retry runs, turn count (number of distinct `tool_use_id` prefixes per session). No new data required; logs already exist. Natural output: CSV or JSON array, one row per session, joinable with sidecar data by session ID prefix. Retry detection: a `PostToolUse` error entry followed by a `PreToolUse` of the same tool type and matching key input (same path for `Read`/`Write`, same command for `Bash`) with no intervening *tool calls* — that is a blind retry and a strong stuck-loop signal. Text-only turns between the failure and the repeat are narrator output, not diagnosis, and should be ignored. Diagnose-then-retry (an intervening tool call of a different type or input) is normal adaptation and should not be flagged.

- **Tool-call duration via Pre/Post join.** The `tools-<session>.jsonl` has both `PreToolUse` and `PostToolUse` entries with matching `tool_use_id` fields. Joining them by `tool_use_id` yields per-call latency. Useful for identifying slow tool classes (long Bash commands vs. fast Reads) and outlier calls that dominated a session's wall time. Can be added to the aggregation script above.

- **Policy effectiveness comparison.** Extend the aggregation script or add standalone: join per-session summaries with sidecar `active_policy` and cost from `ccusage_jsonl`. Surface cost, call count, and session duration per policy. Answers "does `dev` produce cheaper sessions than `strict` for the same class of work?" Dependency: `ended_at` for duration.

- **Cross-session trend analysis.** Plot session-level metrics over time: cost per session, call count per session, session duration, Bash density. A degrading cache ratio (currently visible only in ccusage) or increasing session length is a signal that session hygiene has eroded. The aggregation script output is the input; a simple CSV + `gnuplot` or a Jupyter notebook is sufficient. No new data collection needed.

- **Blocked tool call recording.** When `validate-memory-write` or `validate-settings-write` (now firing live as `vigil-hook` subcommands) blocks a tool call, the `PostToolUse` hook does not fire — only `PreToolUse` fires. The tools JSONL therefore has no record of denied calls. Options: (a) the validator subcommand appends a `{"event":"denied","tool_name":...}` line to `tools-<session>.jsonl` itself before exiting non-zero; (b) the aggregation script compares PreToolUse vs. PostToolUse counts by `tool_use_id` to infer denials. Tracking denied calls adds a security signal: a session with many denied Write attempts is anomalous. More urgent now that the validators actually fire.

- **Subagent cost attribution.** When architect or code-reviewer agents spawn, they create their own JSONL entries (possibly under the same project slug, possibly a separate one). ccusage shows subagent overhead as ~8% of total spend, but `join-sessions.py` and the aggregation script currently treat main-session and subagent JSONL entries as equivalent. Filtering by `isSidechain` excludes some entries already; verifying whether subagent sessions land in the same JSONL file or a different one, and whether `isSidechain` correctly tags them, would let the scripts attribute costs to main vs. agent sessions.

- **Coordinated log pruning.** `vigil-hook prune-logs` (delegating to `scripts/prune-logs.py`) prunes session transcripts (`session-*.txt`) and sidecar JSON (`session-*.json`) by age and total size. It does not prune `tools-<session>.jsonl` files. Now that the per-tool-call hook fires for real, the tools log accumulates one entry per tool call per session and grows independently of the transcript pruning. Options: extend `scripts/prune-logs.py` to include tool JSONL files in the retention calculation, keyed by the same session ID prefix, so the two are pruned together. More urgent post-`b87386d` than it was when written.

- **`vigil-report` static HTML generator.** One-shot CLI: `vigil-report` writes `~/vigil-reports/YYYY-MM-DD.html` — single page, all data baked in (no server, no live queries), sortable tables for sessions / commits / bugs and light charts (Chart.js or similar) for cost trends and cache ratio. Hits ~80% of the value a GUI would provide at ~20% of the cost; if users start asking "can I drill into this in real time?", that's the signal to consider a GUI. Depends on the tool-use aggregation script for per-session rows; presentation layer on top of the deterministic algorithmic layer. Few-day scope. Natural home as a `vigil report` subcommand once the `vigil` CLI wrapper (Stage 2) lands.

- **LLM enrichment layer with stable-ID caching.** Algorithmic outputs are deterministic; LLM passes add semantics where pure code can't (categorize intent, name patterns, explain outliers). Four passes, ordered by value-per-token: (a) per-session summary at session-end — 2 sentences + category tag (`feature`/`fix`/`refactor`/`explore`/`debug`/`docs`/`meta`) + 1-line went-well/went-wrong, cached on the sidecar, makes the corpus browseable; (b) per-(inducing, fix) explanation — LLM reads both diffs and bracketing transcripts, produces 2-sentence "what broke," cached by SHA pair, runs once per pair forever; (c) outlier explanation on demand — interactive, user clicks "why was this session expensive?" and an LLM reads transcript and tool-call log; (d) cross-session pattern detection — monthly LLM pass over per-session summaries (not raw transcripts) finds recurring behavioral patterns, small input → high-leverage output. Architectural rule: LLM never sees raw `tools-<session>.jsonl`; it operates on aggregated rows produced by the deterministic layer. Each pass is independent and ships on its own.

- **`vigil-pyszz` wrapper for in-session triggering** *(only if friction justifies it)*. Today `run-pyszz.sh` runs only outside Vigil sessions because `Bash(python3:*)` is denied (broad anti-arbitrary-code rule, not pyszz-specific). A sudo-installed `/usr/local/bin/vigil-pyszz` binary, narrow argv (only invokes pyszz_v2's `main.py` with a fixed shape), allowed in policy via `Bash(vigil-pyszz:*)`, would let Claude trigger pyszz in-session — same security pattern as `vigil-hook`. Build only if mid-session bug-attribution queries become recurring friction; pyszz is retrospective and manual invocation is fine for now. Optional follow-on: `SessionEnd` hook subcommand of `vigil-hook` that runs `vigil-pyszz` plus the join script automatically.

## Stage 2 — needs versioning / wider design

These widen appeal to less-technical users without compromising the security model. Each removes a friction point but depends on infrastructure (versioning, the `vigil` umbrella CLI) that doesn't exist yet.

- **One-line installer.** `curl -fsSL <url> | bash` — the standard for friend-distributed dotfiles tools. Removes "clone the repo, cd into it, run install.sh" to a single paste. Trade-off: encourages running unread shell. Mitigate with a pre-install banner that names what's about to happen and exits-on-N if interactive.
- **`vigil` CLI wrapper.** Single command for common operations: `vigil update`, `vigil doctor`, `vigil policy dev` (sets default policy via shell function or env var). Centralizes paths users currently have to remember. Pairs naturally with `doctor.sh`.
- **Pre-flight check at install time.** Verify Claude Code is installed, sandbox is supported on the platform, `python3` is available, before the installer makes any changes. Today the installer assumes all this and fails cryptically when wrong. ~20 lines; turns confusing failure into "missing X, run Y to install."
- **Better install.sh error messages.** When bwrap fails, when `python3` is missing, when a path conflict is found — point at `vigil doctor` (once it exists) and a one-line "run X to diagnose." Avoids stranding users at cryptic Linux errors.
- **A "casual" preset profile.** Not strict, not yolo, not dev — tuned for "personal laptop, not handling production data, wants safety but minimal friction." Could become the default for new installs, with `strict` promoted to "for production / shared / sensitive machines." Lowers the floor for users who aren't ready to think about the strict/dev/yolo trade-off.
- **Auto-update mechanism.** `vigil update` fetches latest, runs the update flow that `update.sh` automates. Needs versioning first. Removes manual `git pull` cycle.
- **CI mirror of the local review gate.** Today `architect` and `code-reviewer` agents run pre-commit on the maintainer's machine; the review trail is invisible to a visitor scrolling the repo. Mirror the gate in GitHub Actions so pushes to `main` (and, once PRs open up, PRs) trigger the same checks and block on findings. Turns the "direct-to-main with no visible review" signal into "direct-to-main with an enforced CI gate." Depends on the agents being runnable headless in CI; may need a harness shim.
- **Container- or VM-based isolation mode.** The current sandbox layer is bubblewrap/Seatbelt configuration, which is light compared to a full namespace + cgroup + read-only-root container or a VM. A stronger runtime would close the "what if bubblewrap has a CVE" gap and give Vigil a genuine containment story rather than just a policy story. Design decisions to resolve: how `vigil-dev` handles credentials currently sourced from `$HOME` (bind-mount vs. copy vs. forward), how commit-signing interacts with a containerized `ssh-agent` vs. the host agent the `excludedCommands` carve-out currently reaches, whether this is an alternative runtime behind a flag or a distinct profile family, and how file edits round-trip between container and host without killing ergonomics. Out-of-scope placeholder in `THREAT_MODEL.md` points here. Design exploration done — see `VM_DESIGN.md`.
- **OpenCode permission-layer and hooks port.** OpenCode has a permission system (allow/deny/ask/wildcards, per-agent scoping) and a hook system (`tool.execute.before`/`tool.execute.after`) structurally similar to Claude Code's equivalents. The deny-list config and PreToolUse-style hooks could be ported with mostly mechanical schema mapping. What does not port: OS-level sandboxing (OpenCode has no bubblewrap equivalent; external wrapping or the VM layer would carry that role). What changes in the threat model: Claude's training-level refusal of sensitive reads ("third defense layer," noted in `THREAT_MODEL.md`) is Claude-specific and does not apply when users run OpenCode with non-Claude models. The rules/skills layer already transfers automatically via OpenCode's `~/.claude/` fallback — see `COMPATIBILITY.md`. Prerequisite: decide whether to document weaker-confidence posture honestly or require the VM layer as the outer ring on OpenCode.
- **Cloud / remote-agent environment suitability.** The current design assumes a single-user laptop. Every layer that isn't pure sandbox config carries a laptop assumption: the env-scrub layer runs in the operator's shell before launch, session logs write to `~/vigil-logs/`, the commit-review gate is a pre-push hook on a locally-cloned repo, and `{{HOME}}`/`{{CWD}}` substitutions in deny lists bake in local paths. Making Vigil work in devbox/Codespaces/remote-agent-runner contexts is a scoping exercise first, not a direct port — decide which layers port, which get replaced, and which are cut. Out-of-scope placeholder in `THREAT_MODEL.md` points here.
- **Sigstore / cosign for manifest signing.** The commit-review gate's `.git/review-gate/.manifest` currently uses SHA-256 hashes for tamper detection. Sigstore (with `cosign`) is the standard FOSS approach for signing software artifacts and aligns Vigil with the supply-chain-security ecosystem (SLSA framework, OpenSSF Scorecard). Adoption would extend tamper resistance from hash-based to cryptographic signatures backed by a transparency log, enable verification flows for any future hosted-scanning-service variant, and position Vigil as a reference implementation of paranoid-by-default agent-coding posture using the standards. Concrete: replace or augment the manifest SHA-256 check with a cosign-signed manifest; document the verification flow in `THREAT_MODEL.md`. Pairs naturally with the `vigil-sign-up` helper (which already works the same key-management territory via ssh-agent for commit signing). Aligns the family with cross-project artifact signing — Dashboard application bundles would use the same Sigstore tooling per `~/code/dashboard/BACKLOG.md`.

## Considered and not pursued

- **Bash/zsh/fish completion for `claude --settings`.** Multi-shell completion is a project of its own; not low-hanging despite appearances.
- **Session log rotation / indexing as a structured query layer.** Logs interleave `script(1)` raw bytes with JSON hook output; structured queryability needs a parser. (Simple time-based pruning is in "Next-session candidates" above; structured indexing is a separate, larger effort.)
- **Per-tool deny patterns for "dangerous bash one-liners"** (`rm -rf /`, fork bombs). Pattern-matching theater — bypassed by a one-character change.
- **Wizard-driven policy customization.** Encourages editing without understanding, which is exactly what the security framing tries to prevent. The strict/dev/yolo split is the wizard.
- **Pre-built role profiles** (data scientist, web dev, homelab admin). High maintenance burden, value uncertain, risks creating a sprawl of profiles each tied to a specific stack.
- **Distribution via Brew / APT / package managers.** Real distribution work; premature for current stage.
- **Transcript-extraction utility for collaborative session review.** Extends Vigil's mission beyond security posture; the underlying collaboration problem is better addressed at the claude.ai / Claude Code Web layer. If ever revisited, smallest-footprint implementation is a `SessionEnd` hook parsing the existing `.txt` companion — but only if auditable per-session transcripts for a trusted co-reviewer become a first-class product direction.
- **Chokepoint `git` wrapper for commit-review enforcement.** An earlier variant of the gate wrapped `git` itself to intercept `push` (and optionally `commit`) and route through review. Rejected: the underlying attack — persistent `core.hooksPath` tampering — is blockable at the `denyWrite` sandbox layer (see `scripts/filter-sandbox-denies.py` master tuples), which the shipped design already does. The wrapper would have added an always-on interposer on a load-bearing binary, expanded open-source attack surface, and duplicated coverage the sandbox layer provides — for no additional threat-model coverage.

## Maintenance

When acting on an item, remove it from this file in the same commit (or note its disposition). When rejecting an item, decide whether it's worth preserving as a "why not X?" answer: ideas likely to be re-proposed (because they look obvious from outside) belong in *Considered and not pursued* with a single-sentence rationale — no investigation history, that lives in git. One-off triage decisions that won't be re-proposed stay in commit messages only. The file should shrink as easily as it grows. Re-triage and update the "Last triaged" date when you next return to this file.
